version: "3"

services:
  rz-api:
    image: rinz-mt
    build: .
    restart: always
    volumes:
      - ./:/webapps
    ports:
      - 6005:5005
    command: supervisord -n -c /etc/supervisor.d/service.conf
